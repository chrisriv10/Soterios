# Integration smoke checks

Headless checks that exercise maintenance scheduling, tray health summary, and the updater dev path without launching Electron.

```bash
npm run smoke:integration
```

For unit tests, use `npm test` (see [../README.md](../README.md)).
