/**
 * Global renderer application state.
 *
 * Initialized with nulls and populated at runtime by the router and
 * tool modules.
 */

window.AppState = {
  lastScanSummary: null,
  lastPasswordScore: null,
  currentTheme: null
};
